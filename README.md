# ProjectOne

A self-contained Tetris web app (HTML/CSS/JS only) with:
- Light and dark mode toggle
- Responsive layout sized for modern laptop screens
- Score, level, lines tracking
- Next piece preview, pause, hard drop, and restart

## Run locally

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000>.
